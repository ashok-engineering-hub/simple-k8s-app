apiVersion: v1
kind: Service
metadata:
  name: simple-k8s-app
spec:
  type: NodePort
  selector:
    app: simple-k8s-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001   # ✔ KillerKoda auto-exposed port 30000 → 30010
